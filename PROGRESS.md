# 진행 기록 — 혼자 남겨진 도시, 부산

부산정보영재교육원 중2 팀(김동윤·박찬우·이연우·이선호) 연구 발표 산출물.
**이 파일은 작업이 하나 끝날 때마다 갱신한다.** 맨 아래 «변경 이력»에 날짜와 함께 append 한다.

---

## 0. 한눈에 보기

| 단계 | 내용 | 상태 |
|---|---|---|
| 1 | 디자인 계획 · 컴포넌트 트리 확정 | ✅ 완료 |
| 2 | 데이터 수집 (웹에서 직접 다운로드) | ✅ 완료 |
| 3 | Vite + React + TS + Tailwind 세팅, 오프라인 폰트 | ✅ 완료 |
| 4 | CSV 로더 + zod 스키마 + 가설 판정 순수 함수 + 테스트 | ✅ 완료 (18개 통과) |
| 5 | Chapter 0 — 창문 (Hook) | ✅ 완료 |
| 6 | Chapter 1 — H1 지수 추세 | ✅ 완료 |
| 7 | Chapter 2 — H2 코로플레스 + 산점도 | ✅ 완료 |
| 8 | Chapter 3 — H3 피라미드 | ✅ 완료 |
| 9 | Chapter 4 — 우리 가족의 거리 | ✅ 완료 |
| 10 | Chapter 5 — 제도 안내 (공식 사이트 확인 완료) | ✅ 완료 |
| 11 | Chapter 6 — 연구 노트 · 한계 | ✅ 완료 |
| 12 | 발표 모드 · 접근성 · 오프라인 폰트 · 빌드 검증 | ✅ 완료 |

---

## 1. 확정된 설계

**컨셉**: 산복도로의 창문. 사각형 격자가 유일한 시각 모티프이며 챕터마다 격자 → 선 → 지도 → 피라미드로 변형된다.

**2막 구조**: Ch0~3은 심야 남색의 엄밀한 데이터 구간, Ch4에서 종이색으로 전이하며 같은 데이터를 관람객 본인의 좌표로 되돌려준다.

**판정은 코드가 내린다**: `src/lib/hypothesis.ts` 순수 함수가 채택/부분채택/기각을 계산하고 UI는 결과만 렌더링한다. 데이터가 바뀌면 결론도 자동으로 따라온다.

### 컴포넌트 트리

```
App
├─ PresentModeProvider          # ?present=1 / P키, ←→ 챕터 이동, 폰트 1.25배
├─ DataProvider                 # Context+useReducer: csv 로드 → zod 검증 → status
├─ ScrollNarrative
│  ├─ Ch0_Window                → WindowGrid + CountUp
│  ├─ Ch1_Faster                → IndexTrend + YearSlider + GapSentence + VerdictCard(H1)
│  ├─ Ch2_Where                 → DistrictMap + DistrictCard + OldDowntownToggle
│  │                              + Scatter(추세선·r) + VerdictCard(H2)
│  ├─ Ch3_Who                   → DemoPyramid + AnnotationLine + VerdictCard(H3)
│  ├─ Ch4_MyFamily              → ProfileForm + ResultCard
│  ├─ Ch5_WhatNow               → ProgramCard[] + NeighborChecklist
│  └─ Ch6_ResearchNote          → TeamInfo + VerdictSummaryTable + SourceList + Limitations
└─ 공통: ChartFrame(aria-label+텍스트요약+SourceNote), EmptyState, SectionHeading
```

### 색상 토큰

`--ink #0B1420` / `--slate #33465C` / `--lamp #FFC46B` / `--tide #4E9AA8` / `--paper #EDE8DF` / `--rust #C4553F`

접근성 점검에서 `rustdeep #A94430` 을 하나 추가했다. rust 원색은 종이색 위에서 대비가 3.7:1 이라 본문
크기 글자(4.5:1 필요)에 미달한다. 작은 글자는 rustdeep, 큰 숫자(129·109)는 원색 rust 를 쓴다.

---

## 2. 데이터 (완료)

### 수집 파이프라인 — 재현 방법

```bash
node scripts/fetch_mois.mjs    # 행정안전부 주민등록 통계 CSV 원본 → _raw/mois/
node scripts/fetch_kosis.mjs   # KOSIS OpenAPI JSON 원본 → _raw/kosis/  (.env 의 KOSIS_API_KEY 필요)
node scripts/build_csv.mjs     # 원본 → public/data/*.csv, *.geojson
```

보건복지부 고독사 수치는 API가 없어 보도자료 원문(hwpx)에서 전사해 `scripts/build_csv.mjs` 상단 상수에 넣었다.

### 산출된 파일

| 파일 | 범위 | 출처 |
|---|---|---|
| `01_trend_national_busan.csv` | 2010–2024 전국·부산 | KOSIS 인구총조사 + 행안부 주민등록 |
| `02_lonely_death_trend.csv` | 2019–2024 전국·부산 | 보건복지부 고독사 실태조사 |
| `03_busan_districts.csv` | 2016–2024 × 16개 구·군 | KOSIS + 행안부 |
| `04_lonely_death_demo.csv` | 2022·2023·2024 성별×연령 (**전국만**) | 보건복지부 |
| `05_single_household_demo.csv` | 2019–2024 전국·부산 성별×연령 독거 구조 | 행안부 |
| `busan_sgg.geojson` | 부산 16개 구·군 경계 | 통계청 2013 센서스 경계 |
| `sources.json` | 출처 6종 메타데이터 | — |

### 검산 기록

- 16개 구·군 독거노인 합계 187,176 = 부산시 전체값과 정확히 일치 (2024)
- 2023년 전국 고독사 남성 3,035 + 연령미상 18 = 3,053 → 보도자료값과 일치

### 데이터가 이미 보여주는 것 (최종 판정은 `hypothesis.ts` 가 한다)

- **H1**: 2010→2024 독거노인 증가배율 부산 2.32배 / 전국 2.15배. 10만 명당 고독사 부산 7.44→11.23(1.51배), 전국 5.69→7.66(1.35배).
- **H2**: 노후주택 비율 상위는 중구 40.5%, 동구 40.2%, 영도구 34.7%. 다만 **서구(30.5%)가 원도심 5개구 사이에 끼어들고** 사하·사상은 중위권 → 부분 채택 가능성.
- **H3**: 부산 교차표 미공표로 직접 검증 불가.

### 반드시 화면에 밝혀야 할 한계 3가지

1. 노후주택 공표 구간이 `1979년 이전 / 1980~1989 / 1990~1999`뿐 → **"30년 이상"은 계산 불가**. 이 앱은 1989년 이전 준공(2024년 기준 35년 이상)을 쓴다.
2. `elderly_alone_rate`는 분자가 인구총조사, 분모가 주민등록이라 **기준이 섞여** 있다.
3. 부산의 성별×연령 **고독사** 교차표는 공표되지 않는다. Chapter 3은 전국 고독사 피라미드 옆에 부산 **독거(1인세대) 구조**를 병치하고, 둘이 다른 지표임을 명시한다.
4. 독거노인 2011–2014년은 인구총조사가 없어 값이 비어 있다.
5. 상관관계는 인과관계가 아니다.

---

---

## 2-1. 현재 데이터로 나온 판정 (`npx vite-node scripts/report_verdicts.ts`)

| 가설 | 판정 | 근거 |
|---|---|---|
| **H1** 부산은 전국보다 더 가파르게 고립되는가 | **채택** | 2010→2024 독거노인 2.32배(부산) vs 2.15배(전국) · 2019→2024 10만 명당 고독사 1.51배 vs 1.35배. 2024년 발생률 부산 11.23명 / 전국 7.66명 (1.47배) |
| **H2** 어디가 가장 외로운가 | **채택** | 2024년 독거노인 비율 원도심 5개구 27.8% vs 나머지 23.0% (+4.8%p), 상위 5개 구 중 3곳이 원도심, 노후주택 비율과 상관 r = 0.76 |
| **H3** 누가 혼자 떠나는가 | **검증불가** | 부산의 성별×연령 고독사 교차표 미공표. 추정치를 만들지 않고 전국(50·60대 남성 54%)만 제시하고 부산 독거 구조를 병치 |

> 이 판정은 `src/lib/hypothesis.ts` 순수 함수가 계산한다. 데이터가 갱신되면 결론도 자동으로 바뀐다.

---

## 3. 남은 확인 사항

- ~~**Chapter 5 제도 정보**~~ → 2026-07-25 확인 완료. 근거는 `src/data/programs.ts` 에 URL·확인일과 함께 있다. **발표 직전에 `verifiedOn` 날짜를 보고 한 번 더 확인할 것.**
- **오프라인 폰트**: Pretendard / Gowun Batang / IBM Plex Mono 를 로컬 번들에 포함해야 한다. 발표장 와이파이를 믿을 수 없다.
- **연구일지 ⑦번(인구소멸위험지역)**: 한국고용정보원 원자료로 대조 전까지 앱에 넣지 않는다.
- **KOSIS API 키**: 채팅으로 공유된 키라 발표 후 재발급 권장. `.env` 에 있고 `.gitignore` 처리됨.

---

## 4. 실행 방법

```bash
npm install
npm run dev        # http://localhost:5173
npm test           # 가설 판정 함수 테스트
npm run build      # dist/ 생성
npm run preview    # 빌드 결과 확인. 발표는 이 방식으로 띄운다
```

데이터를 갱신하려면 `npm run data:fetch && npm run data:build`.
보건복지부 고독사 수치만 API가 없어 `scripts/build_csv.mjs` 상단 상수를 직접 고쳐야 한다.

---

## 변경 이력

- **2026-07-27** 4일차 — **판정 기준을 사전 등록본으로 되돌리고, 제출물(보고서·포스터)을 완성했다.**
  - **가장 큰 변경: H1 이 '채택'에서 '부분채택'으로 바뀌었다.** 여태 `judgeH1` 은 '2010→2024 증가배율이 부산 > 전국'
    이기만 하면 채택을 냈다. 그런데 2일차 연구계획서에 등록한 기준은 **'2019년부터의 연평균 증가율이 1%p 이상 높을 것'**
    이었다. 화면이 등록한 기준과 다른 계산을 하고 있었던 것이다. 등록본대로 다시 계산하니
    독거노인 축은 격차 0.11%p 로 **미달**, 고독사 축은 1.76%p 로 충족 → 부분채택.
    기준을 코드 상수(`H1_CRITERION`)로 박아 결과를 보고 못 고치게 했다.
  - 이 판정이 오히려 연구의 결론을 선명하게 만들었다. 독거노인 증가는 전국이 함께 겪는 흐름이라 부산의 특수성이
    아니었고, 부산이 뚜렷하게 앞선 것은 고독사(10만 명당으로 보면 격차 2.46%p)였다.
  - `judgeH2` 에 **민감도 분석**을 넣었다. 등록한 원도심 5개구 목록에는 서구가 빠져 있고 노후 산업지역 2곳이 들어 있다.
    목록을 결과에 맞춰 고치는 것은 규칙 위반이므로 등록본을 그대로 두고, 다른 정의(전통 원도심 4개구 / 5개구+서구)로도
    계산해 결론이 유지되는지 확인했다. 6.2%p · 5.2%p 로 둘 다 유지됐다.
  - 고령화율과의 상관(r = 0.86)이 노후주택과의 상관(r = 0.76)보다 강하다는 것을 확인하고 화면 해석에 넣었다.
    '오래된 집이 고립을 만든다'가 아니라 같은 배경의 다른 표시라는 뜻이다.
  - 테스트 18개 → **23개**. 늘어난 5개는 사전 등록 기준을 지키는지 확인하는 것들이고, 그중 하나는
    '부산이 더 빨라도 격차가 1%p 에 못 미치면 미달로 처리하는가'다. 실제 연구에서 일어난 일을 테스트로 박제했다.
  - `_raw/` 를 저장소에 동봉하기로 했다(그전까지 `.gitignore` 로 통째로 빠져 있었다). 수집 시점 데이터를 고정해 두지
    않으면 재현이 불가능하다. 작업 부스러기(압축 푼 이미지 폴더·탐색용 임시 파일)만 골라서 뺐고, `_raw/README.md` 에
    파일별 출처 표를 붙였다. `.gitattributes` 로 `_raw/** -text` 를 걸어 git 이 줄바꿈을 바꾸지 못하게 했다 —
    EUC-KR CSV 라 바이트가 바뀌면 '원본 그대로'가 아니게 된다.
  - `scripts/build_poster.mjs` 신규 — 포스터(600×900)에 들어가는 숫자를 CSV 에서 직접 계산해 HTML 로 뽑는다.
    포스터·화면·보고서의 숫자가 어긋날 여지를 없애려고 손으로 적지 않았다.
  - 제출물: `제출물/칠면조_보고서.hwpx`, `제출물/칠면조_포스터.jpeg`(600×900), 대시보드 캡처 4장.

- **2026-07-25** 발표 모드 · 접근성 · 빌드 검증 완료. **본 작업 종료.** `README.md` 추가.
  - `src/lib/PresentMode.tsx` 신규. `P` 키 또는 `?present=1` 로 켜지고, `←→` 챕터 이동 · `Home/End` · `Esc` 나가기. 글자 1.25배(`--font-scale`), `.present-hide` 장식 숨김, 현재 위치 표시(7분의 몇 + 챕터 이름), 스크린리더용 `aria-live` 안내. 발표 모드 상태를 주소에 남겨 새로고침해도 유지된다.
  - 버그 1건: 화살표를 빠르게 두 번 누르면 한 칸만 가던 문제(같은 틱의 두 번째 입력이 갱신 전 `index` 를 봄) → `setIndex` 함수형 갱신으로 고침. Ch4 선택 상자에 포커스가 있을 때는 단축키를 가로채지 않는다.
  - **접근성 — 대비 미달을 찾아 고쳤다.** 렌더된 DOM 의 색을 실제로 합성해 WCAG 대비를 계산해 봤더니 흐린 글자들이 기준에 미달했다(종이색 위 `text-ink/45` = 2.95:1, 기준 4.5:1). 흐림 단계의 바닥을 올렸다 — `text-ink/*` → 65, `text-paper/*`·`fill-paper/*` → 55. rust 원색도 종이색 위에서 3.65:1 이라 작은 글자용 `rustdeep #A94430` 을 새로 뒀다. **최종: 텍스트 408개 전부 통과(미달 0개).**
  - 그 밖에 확인: `lang="ko"`, h1 하나에 h2/h3/h4 계층 정상, `role="img"` 6개 전부 `aria-label` 있음, 버튼 7개·입력 7개·링크 전부 접근 가능한 이름 있음, 중복 id 없음, 표에 `caption` 있음, 건너뛰기 링크 추가.
  - 빌드 검증: `dist/` 에 폰트 207개·데이터 7개, 자산 경로 전부 상대경로.
  - **정정**: 예전에 적어 둔 "`base:'./'` 라 파일로 열어도 동작"은 틀렸다. 빌드 결과는 `<script type="module">` 이라 `file://` 로 직접 열면 브라우저가 CORS 로 막는다. 발표는 `npm run preview` 로 로컬 서버를 띄워서 한다. **발표장 노트북에서 반드시 한 번 직접 확인할 것.**
  - 이 환경(브라우저 패널 미표시)에서는 스크롤이 동작하지 않아 **챕터 이동 시 실제 스크롤은 눈으로 확인하지 못했다.** 인덱스·표시·키 입력·글자 크기는 확인됐다. 실제 화면에서 한 번 넘겨 볼 것.

- **2026-07-25** Chapter 6(연구 노트) 완성 — **본문 7개 챕터 전부 연결됨(Ch0~Ch6)**.
  - **판정 요약표를 하드코딩하지 않았다**: `judgeH1/H2/H3` 를 화면에서 다시 호출해 그 결과를 그대로 표로 그린다. 데이터를 갱신하면 발표 마지막 장의 결론도 자동으로 바뀐다. 심사위원이 "이 표는 어디서 나온 값이냐"고 물으면 `hypothesis.ts` 를 열어 보여주면 된다.
  - '어떻게 만들었나' 5단계(가설 먼저 → 스크립트 수집 → 검산 → 순수함수+테스트 → 제도 공식 확인)와 재현 명령어를 실었다.
  - '이 연구가 못 한 것' 6개: 30년 이상 노후주택 계산 불가 / 분자·분모 조사 기준 혼재 / 부산 성별×연령 고독사 미공표 / 2011~2014 결측 / 상관≠인과 / **넣으려다 뺀 인구소멸위험지수**(원자료 대조 실패). 질문받기 전에 우리가 먼저 말한다는 원칙.
  - 출처 전체 목록 — 데이터 8종(`sources.json`)과 제도 4종(`programs.ts`, 확인일 포함)을 한자리에.
  - 팀 정보(부산정보영재교육원 중2 김동윤·박찬우·이연우·이선호)와 마무리 문장.
  - 버그 1건 수정: `judgeH2` 의 근거 문장에서 평균이 `23%` 로 나와 `27.8%` 와 자릿수가 어긋나던 것 → `toFixed(1)` 로 고정(`23.0%`). Ch2 와 Ch6 양쪽에 같이 반영됨.
  - 검증: 타입체크·테스트 18개·`npm run build` 통과. 브라우저에서 판정표 3행 값, 출처 17개 항목, 외부 링크 12개 렌더 확인, 콘솔 에러 없음.

- **2026-07-25** Chapter 5(그래서 지금 무엇을 할 수 있나) 완성. **웹에서 공식 사이트를 직접 열어 확인한 것만** 넣었다. 근거는 전부 `src/data/programs.ts` 에 URL·확인일과 함께 박제돼 있다.
  - 실은 것: **노인맞춤돌봄서비스**(보건복지부 공식 안내 페이지), **독거노인·장애인 응급안전안심서비스**(복지부 보도자료 2024-04-04, 독거노인 소득기준 폐지), **129 보건복지상담센터**(129.go.kr, 복지사각지대·노인학대·정신건강은 24시간), **109 자살예방 상담전화**(복지부 보도자료, 24시간·2024-01-01 통합).
  - **이 챕터의 발견**: 노인맞춤돌봄서비스는 **친족이 아닌 이웃(이해관계인)도 신청할 수 있다**(찾기쉬운 생활법령정보 — 2026년 사업안내 기준). 관람객이 실제로 할 수 있는 행동이 있다는 뜻이라 'NeighborChecklist' 4항목을 전부 이런 제도 근거가 있는 것으로만 채웠다. 미담이나 훈화는 넣지 않았다.
  - **뺀 것과 뺀 이유도 화면에 적었다**: 노인맞춤돌봄 콜센터 1661-2129, 응급안전안심 중앙모니터링센터 1566-3232 → 공식 페이지가 열리지 않아 확인 실패로 제외(검색 요약에만 나오는 번호는 쓰지 않는다). 부산 구·군별 안부살핌 사업 → 구마다 다르고 해마다 바뀌는데 공식 안내 확인 실패로 제외. 대신 공식 문서에 나온 창구(읍·면·동 행정복지센터, 129)만 적었다. 부산 항목은 「부산광역시 고독사 예방 및 사회적 고립가구 지원을 위한 조례」(2019 제정)만 남겼다.
  - 모든 카드에 **확인 날짜**를 찍고, "제도는 바뀝니다. 신청 전에 129나 주민센터에서 다시 확인하세요" 문구를 하단에 뒀다.
  - 검증: 타입체크·테스트 18개·`npm run build` 통과. 브라우저에서 체크박스 동작과 외부 링크 4개(mohw 2 · 129.go.kr · mohw 보도자료) 확인, 콘솔 에러 없음.

- **2026-07-25** Chapter 4(우리 가족의 거리) 완성 — **2막 시작**. 챕터가 종이색 배경을 직접 깔고(`bg-paper text-ink`) 위쪽에 ink→paper 그라데이션 띠를 둬서 전환을 만든다. body 배경을 스크롤로 바꾸는 방식은 쓰지 않았다(관찰자 오작동 시 화면이 깨진다). 강조색은 앰버 대신 rust — 종이색 위에서 앰버는 보이지 않는다.
  - `ProfileForm`: 구·군 / 연령대 / 성별 세 개의 **네이티브 `<select>`**. Radix Select 대신 쓴 이유는 키보드·스크린리더·모바일에서 추가 코드 없이 동작하고 발표장에서 실패할 여지가 적어서다. 세 개를 다 고르면 결과가 나온다.
  - **Chapter 0 의 창문이 여기서 회수된다**: 고른 구의 독거노인 가구를 사각형(1칸 = 100가구)으로 다시 그리고, **붉은 칸 하나**가 관람객이 떠올린 그 한 사람의 자리를 가리킨다. 지목되는 칸은 구 코드 해시로 고정(렌더마다 옮겨 다니지 않는다). `WindowGrid` 에 `fill` · `highlight` 프롭 추가(기존 호출부는 그대로).
  - **윤리 규칙 — 위험을 예측하지 않는다**: "당신의 가족이 위험합니다" 류의 문장은 데이터로 뒷받침되지 않는 겁주기다. 우리 자료에는 개인의 건강·관계·소득이 한 줄도 없다. 그래서 '이 숫자는 확률이 아닙니다' 문단을 화면에 못박고, 확률 대신 **위치**(같은 칸에 몇 명이 서 있는가)만 보여준다. 입력값은 전송·저장하지 않는다는 것도 폼 아래에 적었다.
  - `SourceNote` 신규 — 차트가 아닌 자리(카드·안내문)에서도 출처 표기 규칙을 지키기 위해 `ChartFrame` 에서 출처 줄만 분리. `tone="light"` 로 종이색 배경 대응. Ch5·Ch6 에서도 쓴다.
  - 검증: 타입체크·테스트 18개·`npm run build` 통과. 브라우저에서 중구·70대·여성을 실제로 선택해 확인 — 사각형 40개 중 rust 1칸, "1,000명 가운데 326명", "70대 여성 1인세대 65,691세대 · 10개 칸 중 2번째", 배경 `rgb(237,232,223)` / 글자 `rgb(11,20,32)` 전환 정상, 콘솔 에러 없음.

- **2026-07-25** Chapter 3(H3) 완성. `DemoPyramid`(성별 좌우 피라미드, 격자 사각형이 가로로 누운 형태) 신규. 전국 고독사 피라미드와 부산 1인세대 피라미드를 나란히 놓고, 그 사이에 "부산에서 혼자 사는 사람이 가장 많은 칸은 60대 여성인데 전국에서 혼자 죽는 사람이 가장 많은 칸은 60대 남성"이라는 문장을 데이터에서 직접 뽑아 넣었다. 연도 전환(2022·2023·2024 — 두 표에 모두 있는 연도만).
  - **괄호를 어디에 칠지도 데이터가 정한다**: `topAdjacentPair()` 가 붙어 있는 두 연령대 × 같은 성별 중 합이 가장 큰 묶음을 찾아 괄호와 이름표를 붙인다. 현재 값으로는 고독사 = 50·60대 남성 59.0%, 부산 독거 = 60·70대 여성 29.8%.
  - **분모 문제 처리**: 04 CSV 의 `share_pct` 는 연령미상까지 포함한 전체 대비(50·60대 남성 54.0%)이고 05 는 성별 내 비율이라 두 표의 기준이 다르다. 피라미드 막대는 두 표 모두 **그림에 실린 10개 칸의 합계 = 100** 으로 다시 계산해 같은 규칙으로 맞추고, 판정 카드의 54.0% 와 그림의 59.0% 가 왜 다른지 차트 아래에 한 줄로 밝혔다. 같은 화면의 두 숫자가 어긋나 보이면 심사위원이 가장 먼저 묻는다.
  - **없는 데이터를 없다고 그렸다**: '여기 있어야 할 세 번째 그림' 점선 박스에 부산 성별×연령 고독사 교차표가 미공표라는 사실과, 전국 비율을 부산 건수에 곱한 추정치를 만들지 않기로 한 이유를 적었다. H3 판정은 `judgeH3()` 가 그대로 **검증불가**를 낸다.
  - 검증: 타입체크 통과, 테스트 18개 통과(`hypothesis.ts` 는 건드리지 않음), 브라우저에서 막대 20개·툴팁 실측값 20개 정상 렌더, 콘솔 에러 없음, `npm run build` 성공.
  - `.claude/launch.json` 에 `autoPort: true` 추가 (5173 이 점유돼 있을 때 다른 포트로 뜨도록).

- **2026-07-25** Chapter 2(H2) 완성. `DistrictMap`(d3-geo 직접 투영 코로플레스, 어두운 회청→앰버 단색 램프), 구·군 카드(순위 포함), 원도심 5개구 토글, `Scatter`(노후주택×독거노인, 점 크기=인구, 추세선·r 표시). `03` CSV에 `population` 컬럼 추가.
  - 버그 2건 수정: ① framer-motion 컴포넌트에 직접 단 `onFocus`/`onMouseEnter` 가 제스처 시스템에 가로채여 호출되지 않던 문제 → 상호작용을 일반 `<g>` 로 분리하고 Enter/Space 키 처리 추가. ② SVG 자식마다 `whileInView` 를 걸어 흐려지면 안 되는 구까지 흐려지던 문제 → 뷰포트 판정을 차트당 한 번으로 올림.
  - `src/lib/useReveal.ts` 신규: IntersectionObserver 가 동작하지 않는 환경에서도 화면이 비지 않도록 스크롤 위치 직접 계산 폴백을 함께 둔다.
  - `npm run build` 검증: `dist/` 에 폰트 207개·데이터 7개 정상 포함, 폰트 경로 상대경로 유지.

- **2026-07-25** Chapter 1(H1) 완성. 시그니처 모션 구현 — 격자 사각형이 흩어져 부산 꺾은선 위로 내려앉은 뒤 선이 그려진다(`IndexTrend`, 커스텀 SVG). 지표 전환 탭(독거노인 수 / 10만 명당 고독사), 연도 슬라이더 → 격차 문장 실시간 갱신. 공통 컴포넌트 `ChartFrame`(출처·aria-label·텍스트 대체 요약·주의사항 강제), `VerdictCard`. 값이 있는 연도에만 점을 찍어 2011~2014 공백이 눈에 보이게 했다. Radix 슬라이더의 `role="slider"`가 Thumb에 붙는 문제를 고쳐 스크린리더가 "2023년"을 읽도록 수정.
- **2026-07-25** Chapter 0(창문) 완성. `WindowGrid`(사각형 1개 = 1,000가구, 무작위 순서로 켜짐 · 고정 시드), `CountUp`(탭 전환 시 숫자가 얼어붙지 않도록 안전장치 추가), `EmptyState`, `DataProvider`. 브라우저 확인: 사각형 187개 렌더, 카피 "부산의 밤에 켜지는 불빛 중 18만 7,176개는, 혼자 켜집니다." 타입체크 통과.
- **2026-07-25** Vite + React 18 + TS + Tailwind 세팅. 폰트 206개 파일을 `public/fonts/` 에 로컬 번들(오프라인 대비). `src/data/schema.ts`(zod), `src/lib/loadCsv.ts`, `src/lib/hypothesis.ts` + 테스트 18개 통과. 실데이터 판정 결과: H1 채택 / H2 채택 / H3 검증불가.
- **2026-07-25** 설계 확정. 데이터 수집 완료 (행안부 90개 CSV + KOSIS 2개 표 + 복지부 보도자료 2건 전사 + 경계 GeoJSON). `public/data/` 7개 파일 생성, 검산 통과.
