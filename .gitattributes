# _raw/ 는 '수집 시점의 원본 그대로'가 존재 이유다.
# git 이 줄바꿈을 CRLF 로 바꿔 버리면 바이트가 달라져 스냅샷의 의미가 없어진다.
# 행안부 CSV 는 EUC-KR 이기도 해서 텍스트로 다루지 않는다.
_raw/** -text

# 분석용으로 만든 CSV 는 사람이 읽고 고칠 수 있어야 하므로 LF 로 통일한다.
public/data/*.csv text eol=lf
public/data/*.json text eol=lf
